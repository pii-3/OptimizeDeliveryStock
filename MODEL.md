# 入荷最適化 数学モデル

## 概要
4週分の日別PSIモデルに基づき、複数商品の最適な入荷スケジュールを決定する線形計画問題。

---

## 集合

- **商品の集合**: $P = \{p_1, p_2, \ldots\}$
- **日付の集合**: $D = \{d_1, d_2, \ldots\}$

---

## 意思決定変数

| 変数 | 定義 | 備考 |
|------|------|------|
| $x_{p,d}^{(s)}$ | 商品 $p$、日付 $d$ の小口（ケース）入荷数 | $(p \in P, d \in D)$ |
| $x_{p,d}^{(l)}$ | 商品 $p$、日付 $d$ の大口（トラック）入荷数 | $(p \in P, d \in D)$ |
| $n_d$ | 日付 $d$ のトラック台数 | $(d \in D)$、整数 |
| $I_{p,d}$ | 商品 $p$、日付 $d$ の在庫 | $(p \in P, d \in D)$ |

**コード内での対応** (optimizer.py)
- `x_small[p][d]` = $x_{p,d}^{(s)}$
- `x_large[p][d]` = $x_{p,d}^{(l)}$
- `n_trucks[d]` = $n_d$
- `I[p][d]` = $I_{p,d}$

---

## 入力データ

| パラメータ | 定義 | ソース |
|-----------|------|--------|
| $\bar{x}_{p,d}$ | 商品 $p$、日付 $d$ のベース入荷予定数 | time_series: 入荷予定 |
| $s_{p,d}$ | 商品 $p$、日付 $d$ の出荷予測 | time_series: 出荷予測 |
| $I_{p,0}$ | 商品 $p$ の前日末在庫 | inventory_init: 前日末在庫 |
| $S_{p,d}^{cum}$ | 商品 $p$ が日付 $d$ までに出荷可能な累積数量 | time_series: 出荷可能数累計 |

---

## 定数

| 定数 | 定義 | ソース |
|------|------|--------|
| $h_p$ | 商品 $p$ の1ケース1日当たり在庫保管コスト | product_master: 在庫コスト |
| $c_p^s$ | 商品 $p$ をケース配送した際のケース当たり単価 | product_master: 配送費_ケース |
| $C^l$ | トラック1台当たりの配送費 | parameters: 配送費_車両 |
| $K_p$ | 商品 $p$ のみでトラック配送した際の満車ケース数 | product_master: CS/車両 |

---

## 目的関数

**最小化**: 総配送コスト + 総在庫保管コスト

$$\text{minimize} \quad \sum_{p,d} c_p^s \cdot x_{p,d}^{(s)} + \sum_{d} C^l \cdot n_d + \sum_{p,d} h_p \cdot I_{p,d}$$

**コード**: optimizer.py:L80-93

---

## 制約条件

### 1. 在庫推移 [optimizer.py:L114-129]

**初日**:
$$I_{p,1} = I_{p,0} + x_{p,1}^{(s)} + x_{p,1}^{(l)} - s_{p,1}$$

**2日目以降**:
$$I_{p,d} = I_{p,d-1} + x_{p,d}^{(s)} + x_{p,d}^{(l)} - s_{p,d} \quad (d > 1)$$

### 2. トラック容量（混載対応） [optimizer.py:L99-106]

$$\sum_{p} \frac{x_{p,d}^{(l)}}{K_p} \le n_d \quad (d \in D)$$

各商品の大口入荷を満車ケース数で正規化し、合計が確保すべきトラック台数以下。

### 3. 日次の入荷合計 ≥ ベース入荷予定合計 [optimizer.py:L108-111]

$$\sum_{p} (x_{p,d}^{(s)} + x_{p,d}^{(l)}) \ge \sum_{p} \bar{x}_{p,d} \quad (d \in D)$$

### 4. 累積入荷数 ≥ 累積ベース入荷予定（前倒しのみ可能） [optimizer.py:L131-134]

$$\sum_{\tau=1}^{d} (x_{p,\tau}^{(s)} + x_{p,\tau}^{(l)}) \ge \sum_{\tau=1}^{d} \bar{x}_{p,\tau} \quad (p \in P, d \in D)$$

### 5. 累積入荷数 ≤ 出荷可能数累計 [optimizer.py:L136-139]

$$\sum_{\tau=1}^{d} (x_{p,\tau}^{(s)} + x_{p,\tau}^{(l)}) \le S_{p,d}^{cum} \quad (p \in P, d \in D)$$

---

## 求解

**ソルバー**: CBC（Coin-or-branch-and-cut） via PuLP

**ステータス確認**: optimizer.py:L142-148

---

## 未実装の制約

- 今回発注の納品日以前の入荷禁止（$t_p$ パラメータ）
